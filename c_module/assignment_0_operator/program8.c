#include <stdio.h>

int main() {
    int length, width, perimeter;
	length = 10;
	width = 10;
    perimeter = 2 * (length + width);
    printf("Perimeter of rectangle = %d", perimeter);
    return 0;
}

